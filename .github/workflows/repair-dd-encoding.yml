name: Repair Data Discovery Encoding

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Repair shell encoding
        shell: bash
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = 'assets/js/databyte-discovery-product-app-v4-shell.js';