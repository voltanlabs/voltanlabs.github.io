(function(){let pending=null;document.addEventListener('click',event=>{const card=event.target.closest('#partyList [data-lead],#partyList [data-repo]');if(!card)return;event.preventDefault();event.stopImmediatePropagation();const id=card.dataset.lead||card.dataset.repo;if(!pending){pending=id;card.classList.add('is-selected');return}if(pending!==id)window.DataByteSession.swap(pending,id);pending=null;window.renderParty?.()},true)})();
