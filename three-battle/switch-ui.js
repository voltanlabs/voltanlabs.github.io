(function(){const button=document.getElementById('switchBtn');if(!button)return;function close(modal){modal?.remove()}function open(){const session=window.DataByteSession,battle=window.DataByteBattle,state=battle?.getState?.(),active=session?.starter?.();if(!session||state?.busy||state?.over)return;const choices=session.party?.().filter(item=>item.id!==active&&Number(item.hp??100)>0)||[];const modal=document.createElement('div');modal.className='capture-modal is-open';modal.innerHTML=`<div class="capture-card"><button class="ghost starter-preview-close" data-switch-cancel type="button">CLOSE</button><span class="eyebrow">PARTY SWITCH // SELECT SIGNAL</span><h2>Choose a DataByte</h2><p>Select a healthy party member to enter the battle.</p><div class="switch-list">${choices.length?choices.map(item=>{const progress=session.spriteProgress?.(item)||{xp:Number(item.xp||0),level:Number(item.level||1)};return `<button class="ghost" data-switch-key="${item.uid||item.id}" type="button"><span>${item.name}<small>LV ${progress.level} · ${Math.max(0,Number(item.hp??100))} HP · ${progress.xp} XP</small></span></button>`}).join(''):'<small>No other healthy DataBytes are available.</small>'}</div></div>`;document.body.appendChild(modal);modal.querySelector('[data-switch-cancel]').onclick=()=>close(modal);modal.querySelectorAll('[data-switch-key]').forEach(choice=>choice.onclick=()=>{const key=choice.dataset.switchKey,record=session.party().find(item=>(item.uid||item.id)===key);if(!record||!session.setStarter(record.id))return;close(modal);battle?.switchTurn?.();const log=document.getElementById('battleLog');if(log)log.textContent=`${record.name} entered the battle.`})}button.addEventListener('click',open)})();
