(function(){function render(){const s=window.DataByteSession;if(!s)return;const seen=s.seen().length,captured=s.party().concat(s.repository()).length;document.getElementById('dexSeenCount').textContent=seen;document.getElementById('dexCapturedCount').textContent=captured;const mission=document.getElementById('missionProgress');if(mission)mission.textContent=seen>=3?'Survey complete. Keep scanning for rare signals.':`Discover ${Math.max(0,3-seen)} more signal${3-seen===1?'':'s'} to complete your first survey.`}window.addEventListener('databyte:dex-updated',render);window.addEventListener('databyte:party-updated',render);render()})();
