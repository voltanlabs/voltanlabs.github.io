// assets/js/databyte-discovery-product-app-v3-5.js
// Phase 4.3: Product App transition shell with passive Battle Screen/Controls fallback wiring.
(function(){
  if(!location.pathname.includes('databyte-discovery'))return;
  const K={profile:'vl_databyte_admin_profile_v1',collection:'vl_databyte_discovery_collection_v2',seen:'vl_databyte_seen_v1',party:'vl_databyte_party_v1',items:'vl_databyte_items_v1'};
  const roster=()=>window.DD_CANON_ROSTER||[], enc=()=>window.DD_ENCOUNTER_RUNTIME, cap=()=>window.DD_CAPTURE_RUNTIME, resolver=()=>window.DD_BATTLE_RESOLVER, battle=()=>window.DDBattle24, battleStateRt=()=>window.DD_BATTLE_STATE_RUNTIME;
  const partyRt=()=>window.DD_PARTY_RUNTIME, invRt=()=>window.DD_INVENTORY_RUNTIME, battleScreen=()=>window.DD_BATTLE_SCREEN, battleControls=()=>window.DD_BATTLE_CONTROLS;
  const $=id=>document.getElementById(id), esc=v=>String(v??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
  const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))||f}catch(e){return f}}, write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  const fallbackItems=()=>read(K.items,{byteCoins:8,boosts:3,repairPulses:1}), items=()=>invRt()?invRt().read():fallbackItems();
  const spendItem=(id,amount)=>invRt()?invRt().spend(id,amount):fallbackSpend(id,amount);
  const collection=()=>partyRt()?partyRt().collection():read(K.collection,[]), seen=()=>read(K.seen,[]), party=()=>partyRt()?partyRt().ids():read(K.party,[]);
  const pct=(a,b)=>b?Math.max(0,Math.min(100,Math.round(Number(a||0)/Number(b||1)*100))):0;
  let state={screen:'scanner',battleState:'idle',signal:null,result:null,confirm:null,fx:null,log:'Scanner ready. Enter a code or randomize a signal.'};

  function fallbackSpend(id,amount){let it=fallbackItems();amount=Number(amount||1);if(Number(it[id]||0)<amount)return{ok:false,items:it,spent:0};it[id]=Number(it[id]||0)-amount;write(K.items,it);return{ok:true,items:it,spent:amount}}
  function emit(name,detail){if(battle()&&battle().emit)battle().emit(name,detail)}
  function fx(name){state.fx=name;setTimeout(()=>{if(state.fx===name){state.fx=null;paintFx()}},560)}
  function profile(){let p=read(K.profile,null);if(!p){p={name:'Scanner Admin',rank:'Candidate',createdAt:new Date().toISOString()};write(K.profile,p)}return p}
  function normalize(s){if(!s)return s;s.maxHp=Number(s.maxHp||s.hp||44);s.hp=Number(s.hp??s.maxHp);s.maxStability=Number(s.maxStability||s.stability||8);s.stability=Number(s.stability??s.maxStability);s.atk=Number(s.atk||s.attack||12);s.def=Number(s.def||s.defense||8);s.speed=Number(s.speed||8);const fallback={id:'signal-strike',name:'Signal Strike',power:24,accuracy:92,captureEffect:1,elements:['Signal'],moveType:'attack'};s.moves=Array.isArray(s.moves)&&s.moves.length?s.moves:[fallback];if(resolver())s.moves=s.moves.map(resolver().normalizeMove);return s}
  function lead(){return normalize(partyRt()?partyRt().lead():fallbackLead())}
  function fallbackLead(){let c=collection().map(normalize),p=party();return c.find(s=>p.includes(s.id)&&Number(s.hp)>0)||c.find(s=>Number(s.hp)>0)||c[0]||null}
  function saveSprite(sp){if(partyRt())return partyRt().updateSprite(sp);let c=collection(),i=c.findIndex(x=>x.id===sp.id);if(i>=0){c[i]=sp;write(K.collection,c)}return sp}
  function fillParty(){if(partyRt())return partyRt().autoFill();let c=collection(),p=party().filter(id=>c.some(s=>s.id===id));c.forEach(s=>{if(p.length<5&&!p.includes(s.id))p.push(s.id)});write(K.party,p.slice(0,5));return p.slice(0,5)}
  function mark(s,status){if(!s)return;let a=seen(),i=a.findIndex(x=>(typeof x==='string'?x:x.name)===s.name),r={name:s.name,dex:s.dex,type:s.type,rarity:s.rarity,status,seenAt:new Date().toISOString()};if(i>=0)a[i]=r;else a.push(r);write(K.seen,a)}
  function seed(){profile();if(invRt())invRt().ensure();else if(!localStorage.getItem(K.items))write(K.items,{byteCoins:8,boosts:3,repairPulses:1});if(!collection().length&&roster().length){let base=roster()[0],s=(enc()&&enc().tuneSignal?enc().tuneSignal(base,'STARTER-Leovolt',{starter:true}):base);s=normalize(Object.assign({id:'DBS-STARTER-'+Date.now(),byteCoin:'BC-0001'},s,{rarity:'Starter'}));write(K.collection,[s]);if(partyRt())partyRt().save([s.id]);else write(K.party,[s.id]);mark(s,'Captured')}fillParty()}
  function odds(s){return cap()?cap().odds(s):Number(s&&s.currentChance||30)}
  function setOdds(s,v){return cap()?cap().setOdds(s,v):(s.currentChance=v)}
  function drainSignal(wild,amount,reason){wild.stability=Math.max(0,Number(wild.stability||0)-Number(amount||1));return wild.stability<=0?((reason||'The wild signal')+' collapsed. '+wild.name+' disappeared from scanner range.'):null}
  function stabilizeSignal(wild,amount){wild.stability=Math.min(Number(wild.maxStability||wild.stability||1),Number(wild.stability||0)+Number(amount||1));return wild.stability}
  function isWildDefeated(){return !!(state.signal&&Number(state.signal.hp||0)<=0)}
  function syncBattleState(){let rt=battleStateRt();if(rt&&rt.snapshot)state.battleState=rt.snapshot().value;return state.battleState}
  function pushLog(text){state.log=String(text||'').replace(/Capture Window/g,'Download Window').replace(/Capture window/g,'Download window').replace(/Capture /g,'Download ').replace(/capture /g,'download ')}
  function logList(text){return String(text||'').replace(/Capture Window/g,'Download Window').replace(/Capture window/g,'Download window').replace(/Capture /g,'Download ').replace(/capture /g,'download ').split(/(?<=\.)\s+/).map(x=>x.trim()).filter(Boolean).slice(-5)}
  function finishWildDefeat(wild,notes){wild.hp=0;let rt=battleStateRt();if(rt&&rt.applyWildDefeat){let out=rt.applyWildDefeat(wild,{stabilizeSignal,setOdds,odds,bonus:3});state.battleState=rt.snapshot().value;if(out&&out.ok&&out.value&&out.value.message)notes.push(out.value.message.replace('Choose Capture','Choose Download')+' Download window locked at '+odds(wild)+'%.');}else{state.battleState='victory';if(wild.__defeatProcessed)return;wild.__defeatProcessed=true;stabilizeSignal(wild,1);setOdds(wild,odds(wild)+3);notes.push(wild.name+' is defeated. Download window locked at '+odds(wild)+'%. Choose Download or Return.')}fx('success')}

  function discover(code){code=(code||($('code')&&$('code').value)||'').trim();if(!code){pushLog('Enter a discovery code first.');fx('warn');render();return}let out=enc()?enc().create(code):null;if(!out||!out.signal){pushLog('No signal found. Encounter Runtime unavailable.');fx('warn');render();return}state.signal=normalize(Object.assign({id:'ENC-'+Date.now()},out.signal));state.battleState='idle';if(battleStateRt())battleStateRt().reset('new-encounter');state.screen='encounter';state.result=null;state.confirm=null;mark(state.signal,'Seen');pushLog('Signal locked from '+(state.signal.encounterPoolLabel||'scanner pool')+'.');fx('discover');render()}
  function randomCode(){let c=enc()?enc().randomCode():'DBS-'+Math.random().toString(36).slice(2,7).toUpperCase();if($('code'))$('code').value=c;discover(c)}
  function startBattle(){if(!state.signal)return;if(Number(state.signal.hp||0)<=0){state.battleState='victory';if(battleStateRt())battleStateRt().victory('already-defeated')}else{state.battleState='active';if(battleStateRt())battleStateRt().start(state.signal.id)}state.screen='battle';pushLog('Battle started.');emit('turn',{label:'Battle Start'});fx('battle');render()}
  function resolveHit(user,move,target,mode){return resolver()?resolver().resolve(user,move,target,{mode,seed:mode+'-'+Date.now()}):{hit:true,move:move,type:{label:'neutral'},hpDamage:2,capturePressure:1,notes:[(user.name||'Sprite')+' used '+(move.name||'Move')+'.']}}
  function chooseEnemyMove(wild,l){return resolver()?resolver().chooseEnemyMove(wild,l):(wild.moves&&wild.moves[0])}
  function turnOrder(l,wild){return resolver()?resolver().turnOrder(l,wild):(Number(l.speed)>=Number(wild.speed)?'player':'enemy')}
  function fight(moveId){let wild=normalize(state.signal), l=normalize(lead()); if(!wild||!l)return;let rt=battleStateRt(),block=rt&&rt.shouldBlockAction?rt.shouldBlockAction(wild):{block:(state.battleState!=='active'||Number(wild.hp||0)<=0)};if(block.block){state.battleState='victory';state.signal=wild;pushLog(wild.name+' is already defeated. Choose Download or Return.');fx('warn');render();return}if(l.hp<=0){pushLog('Your lead sprite is fainted. Switch party or return.');fx('warn');render();return}let move=(l.moves||[]).find(m=>m.id===moveId)||(l.moves||[])[0], first=turnOrder(l,wild), notes=[];function player(){if(wild.hp<=0){finishWildDefeat(wild,notes);return true}let d=resolveHit(l,move,wild,'player');if(!d.hit){notes.push(l.name+' used '+d.move.name+', but missed.');emit('warn');fx('warn');return false}wild.hp=Math.max(0,wild.hp-Number(d.hpDamage||0));setOdds(wild,odds(wild)+Number(d.capturePressure||1));notes.push((d.notes&&d.notes[0]||l.name+' attacked.')+' '+wild.name+' HP '+wild.hp+'/'+wild.maxHp+'. Download +'+d.capturePressure+' → '+odds(wild)+'%.');emit('hit',{text:'-'+d.hpDamage+' HP'});fx('hit');if(wild.hp<=0){finishWildDefeat(wild,notes);return true}return false}function enemy(){if(wild.hp<=0)return false;let m=chooseEnemyMove(wild,l), d=resolveHit(wild,m,l,'enemy');if(!d.hit){notes.push(wild.name+' used '+d.move.name+', but missed.');emit('warn');fx('warn');return false}l.hp=Math.max(0,l.hp-Number(d.hpDamage||0));saveSprite(l);notes.push((d.notes&&d.notes[0]||wild.name+' attacked.')+' '+l.name+' HP '+l.hp+'/'+l.maxHp+'.');emit('hit',{text:'-'+d.hpDamage+' HP'});fx('hit');if(l.hp<=0){let collapse=drainSignal(wild,1,l.name+' fainted and the wild signal');if(collapse)return collapse;notes.push(l.name+' fainted. Wild signal weakened to '+wild.stability+'/'+wild.maxStability+'.')}return false}let collapse=null;if(first==='player'){let done=player();if(!done)collapse=enemy()}else{collapse=enemy();if(!collapse)player()}state.signal=wild;syncBattleState();if(collapse)return fail('Signal Disappeared',collapse);pushLog(notes.join(' '));render()}
  function boost(){if(!state.signal)return;if(isWildDefeated()){pushLog('The wild sprite is already defeated. Choose Download or Return.');fx('warn');render();return}if(items().boosts<=0){pushLog('No Signal Boosts left.');fx('warn');render();return}spendItem('boosts',1);if(cap())cap().onBoost(state.signal);else setOdds(state.signal,odds(state.signal)+8);pushLog('Signal Boost used. Download '+odds(state.signal)+'%.');fx('boost');render()}
  function captureAsk(){if(!state.signal)return;state.confirm={odds:odds(state.signal),byteCoins:items().byteCoins};state.screen='confirm';fx('coin');render()}
  function captureResolve(){let it=items(),wild=state.signal;if(!wild)return;if(!cap()||!cap().canAttempt(it)){pushLog('No ByteCoins available.');state.screen='battle';fx('warn');render();return}let out=cap().attempt(wild,it,wild.id);spendItem('byteCoins',1);if(out.ok){wild.byteCoin='BC-'+String(Date.now()).slice(-6);let c=collection();c.push(wild);write(K.collection,c);if(partyRt())partyRt().add(wild);else fillParty();mark(wild,'Captured');return success('Download Complete',wild.name+' downloaded into '+wild.byteCoin+'.')}cap().onFailedCapture(wild);let collapse=drainSignal(wild,1,'Download failed and the wild signal');if(collapse)return fail('Signal Disappeared',collapse);state.signal=wild;state.confirm=null;state.screen='battle';pushLog('Download failed. Roll '+out.roll+' vs '+out.odds+'. Signal weakened to '+wild.stability+'/'+wild.maxStability+'. Download now '+odds(wild)+'%.');fx('fail');render()}
  function repair(){let l=lead();if(!l)return;if(items().repairPulses<=0){pushLog('No Repair Pulses.');fx('warn');render();return}spendItem('repairPulses',1);l.hp=Math.min(l.maxHp,Math.round(l.hp+l.maxHp*.35));saveSprite(l);pushLog('Repair Pulse restored '+l.name+' to '+l.hp+'/'+l.maxHp+' HP.');fx('boost');render()}
  function success(title,msg){state.result={type:'success',title,msg};state.signal=null;state.confirm=null;state.battleState='idle';if(battleStateRt())battleStateRt().reset('result-success');state.screen='result';emit('success');fx('success');render()}
  function fail(title,msg){state.result={type:'fail',title,msg};state.signal=null;state.confirm=null;state.battleState='idle';if(battleStateRt())battleStateRt().reset('result-fail');state.screen='result';fx('fail');render()}
  function back(){if(state.signal&&['encounter','battle','confirm'].includes(state.screen)&&!isWildDefeated()){let collapse=drainSignal(state.signal,1,'You returned and the wild signal');if(collapse)return fail('Signal Disappeared',collapse)}state.screen='scanner';state.battleState='idle';if(battleStateRt())battleStateRt().reset('return');state.signal=null;state.confirm=null;state.result=null;pushLog('Scanner ready.');fx('return');render()}
  function panel(p){state.screen=p;render()}
  function battleContext(){let s=normalize(state.signal),l=normalize(lead());return{lead:l,wild:s,odds:s?odds(s):0,signal:s?Number(s.stability||0):0,maxSignal:s?Number(s.maxStability||1):1,isWildDefeated:isWildDefeated(),latestMessage:state.log,battleState:{value:syncBattleState(),wildDefeated:isWildDefeated(),leadDefeated:l?Number(l.hp||0)<=0:false},moves:l&&l.moves||[]}}
  function metaLine(s){s=normalize(s||{});return `<div class="meta"><span>#${esc(s.dex||'?')} • ${esc(s.rarity||'Common')}</span><span>${esc(String(s.type||'Signal').replace(/\s*\/\s*/g,' • '))}</span></div>`}
  function hpRing(s){s=normalize(s||{});return `<div class="ring hp" style="--p:${pct(s.hp,s.maxHp)}"><div class="avatar"><span>${esc(s.icon||'◇')}</span><b>${s.hp}/${s.maxHp}</b></div></div>`}
  function fighter(s,side){s=normalize(s||{});return `<article class="fighter ${side}">${hpRing(s)}<h2>${esc(s.name||'Unknown')}</h2>${metaLine(s)}</article>`}
  function signalMeter(s){s=normalize(s||{});return `<div class="signalBox"><div><b>Signal</b><span>${s.stability}/${s.maxStability}</span></div><em><i style="width:${pct(s.stability,s.maxStability)}%"></i></em></div>`}
  function downloadGauge(s){let p=odds(s);return `<div class="downloadGauge"><div><b>Download Window</b><span>${p}% / Cap ${s.captureCap||'?'}</span></div><em><i style="width:${pct(p,Number(s.captureCap||100))}%"></i></em></div>`}
  function battleLog(){let rows=logList(state.log).map(x=>`<li>▸ ${esc(x)}</li>`).join('');return `<div class="battleLog"><b>Battle Log</b><ul>${rows||'<li>▸ Awaiting command.</li>'}</ul></div>`}
  function screen(){let s=normalize(state.signal),l=normalize(lead());syncBattleState();if(state.screen==='encounter'&&s)return `<section class="card encounter-card">${fighter(s,'wild single')}<div class="stats"><b>Download ${odds(s)}%</b><b>HP ${s.hp}/${s.maxHp}</b><b>Signal ${s.stability}/${s.maxStability}</b></div><p>${esc(s.lore||'Unknown signal.')}</p><p>${esc(state.log)}</p></section>`;if(state.screen==='battle'&&s){let mod=battleScreen();if(mod&&mod.renderBattleScreen)return mod.renderBattleScreen(battleContext());return `<section class="card battle-card"><div class="battleGrid">${fighter(l||{},'lead')}<strong class="vs">VS</strong>${fighter(s,'wild')}</div>${signalMeter(s)}${downloadGauge(s)}<p class="hint">${isWildDefeated()?'Wild HP is 0. Battle actions are locked; choose Download or Return.':'HP rings show health. Signal is separate encounter stability.'}</p>${battleLog()}</section>`}if(state.screen==='confirm'&&s)return `<section class="card coin-card"><h2>Download ${esc(s.name)}?</h2><div class="coin">BC</div><p>Spend 1 ByteCoin. Download odds ${state.confirm.odds}%.</p><p>On failure, Signal weakens by 1.</p><p>ByteCoins: ${state.confirm.byteCoins}</p></section>`;if(state.screen==='result')return `<section class="card ${state.result&&state.result.type==='success'?'good':'bad'}"><h2>${esc(state.result&&state.result.title||'Result')}</h2><p>${esc(state.result&&state.result.msg||'')}</p></section>`;if(state.screen==='party')return `<section class="card"><h2>Party</h2><p class="hint">Runtime: ${partyRt()?'DD_PARTY_RUNTIME':'fallback local storage'}</p><div class="grid">${(partyRt()?partyRt().members():fillParty().map(id=>collection().find(s=>s.id===id)).filter(Boolean)).map(x=>`<div class="mini">${x.icon} ${x.name}<br>HP ${x.hp}/${x.maxHp}</div>`).join('')||'<p>No downloaded sprites yet.</p>'}</div></section>`;if(state.screen==='items'){let it=items();return `<section class="card"><h2>Inventory</h2><p class="hint">Runtime: ${invRt()?'DD_INVENTORY_RUNTIME':'fallback local storage'}</p><div class="grid"><div class="mini">ByteCoins<br><b>${it.byteCoins}</b></div><div class="mini">Signal Boosts<br><b>${it.boosts}</b></div><div class="mini">Repair Pulses<br><b>${it.repairPulses}</b></div></div></section>`}if(state.screen==='dex'){let capd=new Set(collection().map(x=>x.name)),sn=new Set(seen().map(x=>typeof x==='string'?x:x.name));capd.forEach(x=>sn.add(x));return `<section class="card"><h2>DataByteDex</h2><p>${sn.size}/${roster().length} seen • ${capd.size} downloaded</p><div class="grid">${roster().map(x=>`<div class="mini">${x.icon} #${x.dex} ${x.name}<br>${capd.has(x.name)?'Downloaded':sn.has(x.name)?'Seen':'Unknown'}</div>`).join('')}</div></section>`}if(state.screen==='admin')return `<section class="card"><h2>${esc(profile().name)}</h2><p>Battle Screen Renderer: ${!!battleScreen()}</p><p>Battle Controls Renderer: ${!!battleControls()}</p><p>Battle State Runtime: ${!!battleStateRt()}</p><p>Battle State: ${esc(state.battleState)}</p><p>Encounter Runtime: ${!!enc()}</p><p>Download Runtime: ${!!cap()}</p><p>Battle Resolver: ${!!resolver()}</p><p>Party Runtime: ${!!partyRt()}</p><p>Inventory Runtime: ${!!invRt()}</p></section>`;return `<section class="card scanner-card"><div class="scannerOrb">📡</div><h1>Signal Ready</h1><p>${esc(state.log)}</p></section>`}
  function controls(){if(state.screen==='encounter')return `<button id="battleStart" class="gold">Start Battle</button><button id="back">Return</button>`;if(state.screen==='battle'){let mod=battleControls();if(mod&&mod.renderBattleControls)return mod.renderBattleControls(battleContext());if(isWildDefeated())return `<button id="capture" class="gold">Download</button><button id="back">Return</button>`;let l=lead();return `${(l&&l.moves||[]).slice(0,4).map(m=>`<button class="move" data-m="${esc(m.id)}">${esc(m.name)}<small>PWR ${m.power||0} ACC ${m.accuracy||100}%</small></button>`).join('')}<button id="capture" class="gold">Download</button><button id="boost">Boost</button><button id="repair">Repair</button><button id="back">Return</button>`}if(state.screen==='confirm')return `<button id="confirm" class="gold">Confirm Download</button><button id="battleStart">Back to Battle</button>`;if(state.screen==='result')return `<button id="back" class="gold">Return to Scanner</button>`;if(['party','items','dex','admin'].includes(state.screen))return `<button id="back" class="gold">Return to Scanner</button>`;return `<label>Discovery Code</label><input id="code" placeholder="Enter code..."><button id="discover" class="gold">Discover</button><button id="random">Random Code</button>`}
  function css(){if($('ddv35style'))return;let st=document.createElement('style');st.id='ddv35style';st.textContent=`body>*:not(#ddApp){display:none!important}#ddApp{position:fixed;inset:0;z-index:999999;background:radial-gradient(circle at 50% 30%,rgba(0,123,255,.22),transparent 38%),#07111f;color:white;display:grid;grid-template-rows:auto minmax(0,1fr) auto auto;gap:10px;padding:12px;box-sizing:border-box;font-family:Roboto,system-ui,sans-serif}.top,.card,.controls,.nav button{border:1px solid rgba(125,211,252,.22);background:rgba(7,17,31,.88);border-radius:22px}.top{padding:12px;display:flex;justify-content:space-between;gap:8px}.stage{overflow:auto}.card{padding:16px}.battleGrid{display:grid;grid-template-columns:minmax(0,1fr) 30px minmax(0,1fr);gap:6px;align-items:start}.fighter{text-align:center;min-width:0}.fighter h2{margin:10px 0 6px;color:#007BFF;font-size:clamp(20px,5.4vw,31px);line-height:1.05;overflow-wrap:anywhere}.meta{display:grid;gap:3px;justify-content:center;color:#e0f2fe;font-size:12px;line-height:1.25}.vs{align-self:center;text-align:center}.ring{--p:100;width:min(29vw,116px);height:min(29vw,116px);border-radius:999px;margin:0 auto;display:grid;place-items:center;background:conic-gradient(#22C55E calc(var(--p)*1%),rgba(15,23,42,.94) 0);position:relative}.ring:before{content:"";position:absolute;inset:6px;border-radius:999px;background:#07111f}.avatar{position:relative;z-index:1;width:calc(100% - 22px);height:calc(100% - 22px);border-radius:999px;display:grid;place-items:center;background:rgba(0,123,255,.15);font-size:clamp(28px,8vw,40px);line-height:1}.avatar b{font-size:10px;color:#BAE6FD;margin-top:-12px}.signalBox,.downloadGauge,.battleLog{margin:10px 0 8px;padding:10px;border:1px solid rgba(96,165,250,.25);border-radius:16px;background:rgba(15,23,42,.55)}.signalBox div,.downloadGauge div{display:flex;align-items:center;justify-content:space-between;gap:8px}.signalBox span,.downloadGauge span,.downloadGauge b{color:#FFD700}.signalBox em,.downloadGauge em{display:block;margin-top:6px;height:8px;border-radius:999px;background:#020617;overflow:hidden}.signalBox i{display:block;height:100%;background:linear-gradient(90deg,#38BDF8,#A78BFA,#FB7185);transition:width .25s}.downloadGauge i{display:block;height:100%;background:linear-gradient(90deg,#FFD700,#A3E635,#22C55E);transition:width .25s}.battleLog b{color:#FFD700}.battleLog ul{list-style:none;padding:0;margin:6px 0 0;display:grid;gap:3px;color:#BAE6FD;font-size:13px;line-height:1.25}.stats,.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(105px,1fr));gap:8px}.controls{padding:10px;display:grid;gap:8px}.controls input{padding:14px;border-radius:16px;background:#020617;color:white;border:1px solid rgba(255,255,255,.16)}button{border:0;border-radius:16px;padding:12px;color:white;background:#0F172A;font-weight:900}.gold{background:#FFD700!important;color:#111827!important}.nav{display:grid;grid-template-columns:repeat(5,1fr);gap:6px}.nav button{font-size:11px;padding:9px 3px}.move small{display:block;color:#BAE6FD}.battleMoves,.battleActions{display:grid;gap:8px}.battleActions{grid-template-columns:repeat(2,minmax(0,1fr))}.battleActions .gold{grid-column:1/-1}.log,.hint{color:#BAE6FD}.hint{font-size:12px;line-height:1.3}.mini{background:rgba(15,23,42,.8);border:1px solid rgba(255,255,255,.1);border-radius:14px;padding:10px}.coin,.scannerOrb{width:90px;height:90px;border-radius:999px;display:grid;place-items:center;margin:auto;font-size:36px}.coin{background:#FFD700;color:#111827}.scannerOrb{background:rgba(0,123,255,.15);border:1px solid #38BDF8}.good{border-color:rgba(34,197,94,.65)}.bad{border-color:rgba(251,113,133,.65)}.fx-hit .battle-card{animation:none!important}.fx-success .ring,.fx-boost .signalBox{animation:ddPulse .5s ease}.fx-fail .card,.fx-warn .card{box-shadow:0 0 48px rgba(251,113,133,.22)}@keyframes ddPulse{0%{filter:none}50%{filter:drop-shadow(0 0 18px rgba(255,215,0,.25))}100%{filter:none}}`;document.head.appendChild(st)}
  function paintFx(){let root=$('ddApp');if(root)root.className=state.fx?'fx-'+state.fx:''}
  function action(btn){let a=btn.dataset.action;if(a==='move')return fight(btn.dataset.moveId||btn.dataset.moveIndex);if(a==='download')return captureAsk();if(a==='items')return panel('items');if(a==='switch')return panel('party');if(a==='return')return back()}
  function bind(){if($('discover'))$('discover').onclick=()=>discover();if($('random'))$('random').onclick=randomCode;if($('battleStart'))$('battleStart').onclick=startBattle;document.querySelectorAll('.move').forEach(b=>b.onclick=()=>b.dataset.action?action(b):fight(b.dataset.m));document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>action(b));if($('capture'))$('capture').onclick=captureAsk;if($('confirm'))$('confirm').onclick=captureResolve;if($('boost'))$('boost').onclick=boost;if($('repair'))$('repair').onclick=repair;if($('back'))$('back').onclick=back;document.querySelectorAll('[data-panel]').forEach(b=>b.onclick=()=>panel(b.dataset.panel))}
  function render(){css();if(!$('ddApp'))document.body.innerHTML='<div id="ddApp"><header class="top"><b>Data Discovery</b><span>Phase 4.3 Modular UI</span></header><main id="stage" class="stage"></main><section id="controls" class="controls"></section><nav class="nav"><button data-panel="scanner">Scan</button><button data-panel="dex">Dex</button><button data-panel="party">Party</button><button data-panel="items">Items</button><button data-panel="admin">Admin</button></nav></div>';$('stage').innerHTML=screen();$('controls').innerHTML=controls();bind();paintFx()}
  seed();render();
})();